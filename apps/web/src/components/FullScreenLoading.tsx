import { RioReplyLogo } from "~/components/RioReplyLogo";

export const FullScreenLoading = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <RioReplyLogo size={48} className="mx-auto" />
    </div>
  );
};
